list of all user posts<BR><BR>

<%= render "global/new_post_form" %>

<% @posts.each do |post| %>

  <%= post.user.name %><BR><BR>

  <%= post.post_content %><BR><BR>

  <%= commontator_thread(post) %>

<% end %>

<%= paginate @posts %>